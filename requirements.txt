# nowcast — ML + inference track dependencies.
# Install:  python -m pip install -r requirements.txt
# Torch: for CUDA wheels use the official index, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# --- core scientific stack ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
xarray>=2024.3.0
dask[array]>=2024.5.0
zarr>=2.17,<3
netCDF4>=1.6.5
h5py>=3.10
h5netcdf>=1.3

# --- geospatial / DEM ---
rioxarray>=0.15
rasterio>=1.3.9
pyproj>=3.6
shapely>=2.0
pysheds>=0.4

# --- meteorology / derived signals ---
metpy>=1.6

# --- ingestion orchestration ---
prefect>=2.19

# --- ML: baseline ---
lightgbm>=4.3
scikit-learn>=1.4

# --- ML: deep model ---
torch>=2.2
lightning>=2.2
torchmetrics>=1.3

# --- experiment tracking ---
mlflow>=2.12

# --- config / validation ---
pydantic>=2.6
pyyaml>=6.0

# --- remote fetch (GPM IMERG, Copernicus GLO-30 DEM) ---
requests>=2.31
earthaccess>=0.9
pooch>=1.8

# --- dev / test ---
pytest>=8.1
pytest-cov>=5.0
ruff>=0.4
